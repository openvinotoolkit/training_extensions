data_format: ???
data_root: ${base.data_dir}

mem_cache_size: 1GB
mem_cache_img_max_size: null
image_color_channel: RGB

tile_config:
  enable_tiler: false

train_subset:
  subset_name: train
  num_workers: 2
val_subset:
  subset_name: val
  num_workers: 2
test_subset:
  subset_name: test
  num_workers: 2
