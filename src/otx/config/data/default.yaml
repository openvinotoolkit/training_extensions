data_format: ???
data_root: ${base.data_dir}

mem_cache_size: 1GB
mem_cache_img_resize: ${as_int_tuple:1333,800}

train_subset:
  subset_name: train
  num_workers: 2
val_subset:
  subset_name: val
  num_workers: 2
test_subset:
  subset_name: test
  num_workers: 2
