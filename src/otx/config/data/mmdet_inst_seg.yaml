defaults:
  - default
  - base_inst_seg

data_format: coco_instances
include_polygons: true

train_subset:
  batch_size: 8
  transform_lib_type: MMDET
val_subset:
  batch_size: 8
  transform_lib_type: MMDET
test_subset:
  batch_size: 8
  transform_lib_type: MMDET
