defaults:
  - default

image_color_channel: BGR

train_subset:
  transform_lib_type: TORCHVISION
  transforms:
    - _target_: torchvision.transforms.v2.ToImage
  batch_size: 1
val_subset:
  transform_lib_type: TORCHVISION
  transforms:
    - _target_: torchvision.transforms.v2.ToImage
  batch_size: 1
test_subset:
  transform_lib_type: TORCHVISION
  transforms:
    - _target_: torchvision.transforms.v2.ToImage
  batch_size: 1
