defaults:
  - default

data_format: imagenet_with_subset_dirs

subsets:
  train:
    batch_size: 8
    num_workers: 2
    transform_lib_type: MMPRETRAIN
  val:
    batch_size: 8
    num_workers: 2
    transform_lib_type: MMPRETRAIN
