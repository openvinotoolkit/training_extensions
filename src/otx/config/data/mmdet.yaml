defaults:
  - default

data_format: coco_instances

subsets:
  train:
    batch_size: 8
    num_workers: 2
    transform_lib_type: MMDET
  val:
    batch_size: 8
    num_workers: 2
    transform_lib_type: MMDET
  test:
    batch_size: 8
    num_workers: 2
    transform_lib_type: MMDET
