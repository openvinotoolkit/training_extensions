defaults:
  - base_config
  - base: default
  - callbacks: null
  - data: default
  - trainer: default
  - model: mmdet
  - logger: csv
  - _self_

hydra:
  searchpath:
    - pkg://otx
  runtime:
    output_dir: ${base.output_dir}
