defaults:
  - default

_target_: otx.core.model.module.classification.OTXMulticlassClsLitModule

otx_model:
  _target_: otx.core.model.entity.classification.MMPretrainMulticlassClsModel
  config: ???

# compile model for faster training with pytorch 2.0
torch_compile: false

export_config:
  format: OPENVINO
  precision: FP32
  resize_mode: standard
  pad_value: 0
  swap_rgb: false
