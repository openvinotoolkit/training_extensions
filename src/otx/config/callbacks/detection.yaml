defaults:
  - default

model_checkpoint:
  dirpath: ${base.output_dir}/checkpoints
  filename: "epoch_{epoch:03d}"
  monitor: "val/map"
  mode: "max"
  save_last: True
  auto_insert_metric_name: False

early_stopping:
  monitor: "val/map"
  patience: 100
  mode: "max"
