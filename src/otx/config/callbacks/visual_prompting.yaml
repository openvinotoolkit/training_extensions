defaults:
  - default

model_checkpoint:
  dirpath: ${base.output_dir}/checkpoints
  monitor: "val/Dice"
  mode: "max"
  save_last: True
  verbose: True

early_stopping:
  monitor: "val/Dice"
  mode: "max"
  verbose: True
