# @package _global_
defaults:
  - override /base: segmentation
  - override /callbacks: segmentation
  - override /data: openvino
  - override /model: mmseg

data:
  data_format: common_semantic_segmentation_with_subset_dirs
  test_subset:
    batch_size: 64

model:
  otx_model:
    _target_: otx.core.model.entity.segmentation.OVSegmentationModel
    config:
      model_name: /home/kprokofi/training_extensions_2/training_extensions/outputs/exported_models/segnext_s_kv_large/exported_model.xml
      async_inference: True
      use_throughput_mode: True
      model_type: "Segmentation"
      decode_head:
        num_classes: 2
    num_classes: 2
